http://localhost {
    root * /var/www/html
    file_server

    header {
        X-Lab-Name "Ansible + Caddy"
    }

    log {
        output file {{ log.file }}
        format json
        level {{ log.level }}
    }
}
